{
  "_comment": "Strict ESLint config for NEW code and critical packages",
  "_usage": "Use this config for packages/simulation, packages/storage, packages/core",
  "rules": {
    "@typescript-eslint/no-explicit-any": "error",
    "@typescript-eslint/no-unused-vars": "error",
    "no-console": ["error", { "allow": ["warn", "error", "info"] }],
    "eqeqeq": ["error", "always"],
    "no-var": "error",
    "prefer-const": "error",
    "no-throw-literal": "error"
  }
}
