[Unit]
Description=B2 Backup Timer (every 6 hours)
Requires=b2-sync-opn.service

[Timer]
# Run every 6 hours
OnCalendar=*-*-* 00,06,12,18:00:00
# If system was off, run 5 minutes after boot
OnBootSec=5min
# Run immediately if missed schedule
Persistent=true

[Install]
WantedBy=timers.target
