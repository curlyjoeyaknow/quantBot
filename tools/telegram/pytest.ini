[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = 
    -v
    --strict-markers
    --tb=short
markers =
    unit: Unit tests (fast, no I/O)
    integration: Integration tests (may use DuckDB, files)
    slow: Slow tests (skip in CI)

