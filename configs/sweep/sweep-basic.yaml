# Basic Sweep Configuration
# Minimal example with default values

# Input data
callsFile: calls.json
overlaySetsFile: overlays.json

# Output directory
out: out/sweep-basic

# Grid parameters (3 × 2 × 1 = 6 scenarios)
intervals:
  - 1m
  - 5m
  - 1h

lagsMs:
  - 0
  - 10000

# All other parameters use defaults:
# - entryRule: next_candle_open
# - timeframeMs: 86400000 (24 hours)
# - takerFeeBps: 30 (0.30%)
# - slippageBps: 10 (0.10%)
# - notionalUsd: 1000
# - resume: false

