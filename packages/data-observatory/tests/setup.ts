/**
 * Test Setup
 *
 * Shared setup for all data observatory tests.
 */

import { beforeAll, afterAll } from 'vitest';

beforeAll(() => {
  // Setup if needed
});

afterAll(() => {
  // Cleanup if needed
});

