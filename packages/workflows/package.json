{
  "name": "@quantbot/workflows",
  "version": "1.0.0",
  "description": "Workflow orchestration services (Ichimoku, text workflows, etc.)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@quantbot/core": "workspace:*",
    "@quantbot/utils": "workspace:*",
    "@quantbot/storage": "workspace:*",
    "@quantbot/ohlcv": "workspace:*",
    "@quantbot/token-analysis": "workspace:*",
    "@quantbot/ingestion": "workspace:*",
    "@quantbot/events": "workspace:*",
    "@quantbot/simulation": "workspace:*",
    "pg": "^8.13.0"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "@vitest/coverage-v8": "^4.0.15",
    "typescript": "^5.9.3",
    "vitest": "^4.0.15"
  }
}

