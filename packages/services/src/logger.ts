/**
 * Services Package Logger
 * =======================
 * Centralized logger for the services package with namespace '@quantbot/services'
 */

import { createPackageLogger } from '@quantbot/utils';

export const logger = createPackageLogger('@quantbot/services');

