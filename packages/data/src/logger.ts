/**
 * Data Package Logger
 * ===================
 * Centralized logger for the data package with namespace '@quantbot/data'
 */

import { createPackageLogger } from '@quantbot/utils';

export const logger = createPackageLogger('@quantbot/data');

