# QuantBot Configuration
# ======================
# This file configures database paths and other settings
# Priority: config.yaml > DUCKDB_PATH env var > default

# DuckDB Database Configuration
duckdb:
  # Path to DuckDB database file (default: data/alerts.duckdb)
  # This is the primary database containing canon.alerts_std view
  path: data/alerts.duckdb

